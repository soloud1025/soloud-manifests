apiVersion: v1
kind: Service
metadata:
  name: soloud-was
  namespace: app
  labels:
    app: was
spec:
  type: ClusterIP
  selector:
    app: was
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

